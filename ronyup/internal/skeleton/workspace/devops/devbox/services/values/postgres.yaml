# Dev defaults — change auth.* for your environment (see devbox README).
auth:
  username: dbUser
  password: dbPass
  database: user-db

primary:
  persistence:
    enabled: false

service:
  type: ClusterIP
