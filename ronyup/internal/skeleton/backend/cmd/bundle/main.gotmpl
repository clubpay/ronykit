package main

import "{{.RepositoryPath}}/cmd/runner"

func main() {
	runner.Execute(runner.Config{
		ApplicationName: "{{.ApplicationName}}",
		DisplayName:     "{{.ApplicationName}} ({{.BundleName}})",
		PackageName:     "{{.PackageName}}",
	})
}
