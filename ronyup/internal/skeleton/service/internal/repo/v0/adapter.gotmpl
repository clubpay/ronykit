package v0repo

import (
	"go.uber.org/fx"
	"{{.RepositoryPath}}/{{.PackagePath}}/internal/repo"
)

//nolint:gochecknoglobals
var Init = fx.Options(
	fx.Provide(
		fx.Private,
		fx.Annotate(
			NewItemRepository,
			fx.As(new(repo.ItemRepository)),
		),
	),
)
